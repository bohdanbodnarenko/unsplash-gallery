import React from 'react'

import './Spinner.css'

const Spinner = (props) => {
  return (
    <div className='backDrop'>
      <div class="loader">Loading...</div>
    </div>
  )
}

export default Spinner
